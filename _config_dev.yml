# Local-preview-only override. Never used for production or public staging builds.
# Usage: bundle exec jekyll serve --config _config.yml,_config_dev.yml --host 127.0.0.1 --port 4000 --livereload
coming_soon: false
